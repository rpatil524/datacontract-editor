# npm install -g license-checker
SELF="$(node -p "require('./package.json').name + '@' + require('./package.json').version")"
license-checker --production --unknown --onlyAllow="MIT;Apache-2.0;BSD-3-Clause;ISC;0BSD" --excludePackages="$SELF"
